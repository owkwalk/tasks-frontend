yarnをインストールする。

```
% npm install -g yarn
```

yarnがインストールされたか、確認する。

```
% yarn -v
```

git cloneした後、「task-frontend」フォルダ内に入り、パッケージをインストールする。

```
% yarn
```

ローカルサーバーを起動する。

```
% yarn start
```

サイトをブラウザ表示する。

```
http://localhost:3000
```
